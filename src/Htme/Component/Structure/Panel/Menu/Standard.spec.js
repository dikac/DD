QUnit.test( "Htme.Component.Structure.Panel.Menu.Standard construct", function( assert ) {

    let structure = new Htme.Plugin.Container.Structure.Structure(
        null,
        new Htme.Plugin.Container.Plugin()
    );

    let standard = new Htme.Component.Structure.Panel.Menu.Standard(structure);
});

